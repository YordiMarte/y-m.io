import { PostLayout } from '@/components/PostLayout'
import { createPostMetadata } from '@/lib/posts'

export const post = {
  type: 'GitHub',
  author: 'Brian Ruiz',
  date: '2026-11-25',
  title: 'effectis — Redis clone in TypeScript',
  description:
    'A TypeScript Redis clone built with a functional model and a focused developer experience. View the GitHub repository to explore the project.',
  externalLink: 'https://github.com/YordiMarte/effectis',
  externalLabel: 'View GitHub repository',
}

export const metadata = createPostMetadata(post)

export default (props) => <PostLayout post={post} {...props} />

The early days of living in New York as a software engineer — settling in, building a rhythm, and figuring out how work and creative projects fit into city life.
